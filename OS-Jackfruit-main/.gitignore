solved

# Build artifacts (user-space)
boilerplate/engine
boilerplate/memory_hog
boilerplate/cpu_hog
boilerplate/io_pulse

# Kernel module build artifacts
*.ko
*.o
*.mod
*.mod.c
Module.symvers
modules.order
*.cmd
.tmp_versions/

# Root filesystems - structure tracked with .gitkeep, but exclude base and large files
rootfs-base/
!boilerplate/rootfs-*/
boilerplate/rootfs-*/**/*
!boilerplate/rootfs-*/.gitkeep
*.tar.gz

# Logs - structure tracked with .gitkeep, but exclude log files
*.log
!boilerplate/logs/.gitkeep

# Runtime socket
/tmp/mini_runtime.sock

# Editor / OS junk
*.swp
*~
.vscode/
